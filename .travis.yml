language: java
jdk:
  - oraclejdk8
before_script:
  - chmod +x gradlew
script:
  - ./gradlew check
  - ./gradlew codeCoverageReport
  - ./gradlew clean spotlessApply build assemble && ./docker-build.sh
after_success:
  - bash <(curl -s https://codecov.io/bash)